name: Release
permissions:
  contents: read
  packages: write
on:
  push:

jobs:
  publish:
    permissions:
      contents: read
      packages: write
    uses: Lua-cat/ci-templates/.github/workflows/nuget-minver.yml@main
    secrets: inherit
