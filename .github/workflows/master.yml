name: Release
permissions:
  contents: read
  packages: write
on:
  push:
    tags:
      - "v*"

jobs:
  publish:
    uses: Lua-cat/ci-templates/.github/workflows/nuget-minver.yml@main
    secrets: inherit
    permissions:
      contents: read
      packages: write
